extends layouts/main

block content
    .form-container
        form(action=`/users${routes.changePassword}`, method="post")
            input(type="password", placeholder="oldPassword" name="Old Password")
            input(type="password", placeholder="newPassword" name="New Password")
            input(type="password", placeholder="newPassword1" name="Verify New Password")
            input(type="submit", value="Change Password")